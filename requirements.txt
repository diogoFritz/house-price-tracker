pandas==2.3.2
seaborn==0.13.2
notebook==6.5.4
requests==2.31.0
beautifulsoup4==4.12.2

selenium==4.46.0
undetected-chromedriver==3.5.5
2captcha-python==2.0.9
geopandas==1.1.4
tqdm==4.68.4
python-dotenv==1.1.0

# Criar nvo ambiente virtual python -m venv .venv
# 1) Ativar ambiente virtual .venv\Scripts\Activate
# 2) Instalar dependências pip install -r requirements.txt
# pip install -r requirements.txt --dry-run verifica o que vai ser modificado

# pip freeze > requirements.txt atualiza o ficheiro com as libs instaladas
# pip list mostra as libs instaladas